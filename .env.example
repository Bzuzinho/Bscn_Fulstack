# Example env file (do not commit secrets)
# Copy to .env and fill secrets for local development

# Postgres / Neon
DB_CONNECTION=pgsql
DB_HOST=ep-bitter-glade-agamupv1-pooler.c-2.eu-central-1.aws.neon.tech
DB_PORT=5432
DB_DATABASE=neondb
DB_USERNAME=neondb_owner
# DB_PASSWORD=your_password_here

# Constructed DATABASE_URL used by the server
# DATABASE_URL=postgresql://<user>:<pass>@<host>:<port>/<db>?sslmode=require

REPLIT_DOMAINS=localhost
REPL_ID=dev
SESSION_SECRET=your_session_secret_here
PORT=5000
